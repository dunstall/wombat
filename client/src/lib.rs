pub mod connection;
pub mod consumer;
pub mod producer;

#[cfg(test)]
mod tests {
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
    #[test]
    fn it_works_2() {
        assert_eq!(2 + 2, 4);
    }
}
