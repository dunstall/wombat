// use std::net::SocketAddr;
use std::thread;

use wombat::producer::Producer;
use wombat::connection::Connection;
use wombat::connection::WSConnection;

fn main() {
    println!("producer running");

/*     let mut conn = WSConnection::new() */

    // let mut conn = match WSConnection::connect() {
        // Ok(conn) => conn,
        // Err(_) => return,
    // };

    // loop {
        // conn.send(&[1, 2, 3]);
        // thread::sleep_ms(2000);
    /* } */
}
